Page-header configuration (text / scope / odd-even split).
``tool_setup_headers`` is the only safe fix. needs_human=true if
the rule is ``info`` severity AND the user explicitly chose
header_footer.enabled=false in their profile.
