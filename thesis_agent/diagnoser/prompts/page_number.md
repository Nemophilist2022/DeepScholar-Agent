Page-number format / position violation. Use:
- ``tool_setup_page_numbers`` for full configuration
- ``tool_setup_page_numbers_strict`` when only existing sections
  should be touched (单独改页码 mode)

If the document only has one section, needs_human=true — the user
must decide whether to add a section break.
