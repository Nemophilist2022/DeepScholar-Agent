"""Aliased to check_body for v0.1.

R1.3 lists "fonts" as a separate dimension; we'll split it out when
heading-level / caption-level font rules grow beyond what check_body
covers. For now this module exists so the directory layout matches the
design doc, but it adds no extra registrations.
"""
